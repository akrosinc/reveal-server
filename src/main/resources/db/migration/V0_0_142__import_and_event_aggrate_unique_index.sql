CREATE UNIQUE INDEX ON import_aggregate_string_count(id);

CREATE UNIQUE INDEX ON import_aggregate_numeric(id);

CREATE UNIQUE INDEX ON event_aggregate_numeric(id);

CREATE UNIQUE INDEX ON event_aggregate_string_count(id);
