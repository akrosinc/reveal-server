package com.revealprecision.revealserver.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FilterConfig {

//  @Bean
//  public FilterRegistrationBean<HeaderAddingFilter> customHeaderFilterRegistration() {
//    FilterRegistrationBean<HeaderAddingFilter> registrationBean = new FilterRegistrationBean<>();
//    registrationBean.setFilter(new HeaderAddingFilter());
//    registrationBean.addUrlPatterns("/api/v1/entityTag/filter-sse"); // Adjust URL patterns as needed
//    registrationBean.setOrder(0); // Ensure it runs before other filters
//    return registrationBean;
//  }
}
